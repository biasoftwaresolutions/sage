## sage — Personal Knowledge Wiki

Wiki at `~/sage/`. Sources in `~/sage/sources/`, compiled wiki in `~/sage/wiki/`.
Ops script (no LLM needed): `python3 ~/sage/scripts/sage-operations.py`

| User says | Action |
|-----------|--------|
| **sage ingest** | Read `~/sage/skills/sage-ingest.md` then follow it exactly |
| **sage lint** | Read `~/sage/skills/sage-lint.md` then follow it exactly |
| **sage research** | Read `~/sage/skills/sage-research.md` then follow it exactly |
| **sage query `<topic>`** | Search `~/sage/wiki/index.md` or call `GET /api/context?topic=<topic>` if server running on :3000 |
| **sage capture `<project>`** | Read `~/sage/skills/sage-capture.md` then follow it exactly |
| **sage init** | Read `~/sage/skills/sage-init.md` then follow it exactly |
| **sage remember `<thing>`** | Append entry to `~/sage/wiki/sage-memory/MEMORY.md` immediately, no confirmation needed |
| **sage relocate `<path>`** | Read `~/sage/skills/sage-relocate.md` then follow it exactly |
| **sage meeting** | Read `~/sage/skills/sage-meeting.md` then follow it exactly |
| **sage memory** | Read `~/sage/skills/sage-memory.md` then follow it exactly |

Otherwise, don't interfere — just be a normal assistant.
